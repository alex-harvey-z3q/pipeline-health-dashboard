apiVersion: v1
kind: ConfigMap
metadata:
  name: pipeline-health-dashboard-config
data:
  config.yml: |
    # Replace with a real configuration before deploying.
    organization_url: https://dev.azure.com/your-organization
    projects:
      - name: "REPLACE: project name"
        pipelines: []
